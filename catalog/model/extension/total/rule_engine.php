<?php
class ModelExtensionTotalRuleEngine extends Model {

    public function getTotal($total) {
        // $this->load->model('marketing/rule_engine');
        
        // $rules = $this->model_marketing_rule_engine->getRuleEngines();

        // foreach ($rules as $rule) {
        //     $code = $rule['code'];
        //     if (method_exists($this, $code)) {
        //         $this->$code($total);
        //     }
        // }


        // $total['totals'][] = [
        //             'code'  => 'loyalty_points',
        //             // 'title' => 'Loyalty Points (' . $points_used . ')',
        //             // 'value' => -$discount,
        //             // 'loyalty_points_used' => $points_used,
        //             'sort_order' => $this->config->get('total_loyalty_points_sort_order')
        //             ];

        //             // $total['total'] -= $discount;

      

    }

}